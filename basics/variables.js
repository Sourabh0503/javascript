const id=123;  // changes not allowed
let number=124;  // good to use
var myName="sourabh"  //there is no scope for var like change can happen anywhere
city="faridabad" // very bad practice

console.table([id,number,myName,city]);